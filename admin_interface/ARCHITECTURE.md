# 🔄 Architecture - Interface Admin SmartBin v2.0

## Flux de Données

```
┌─────────────────────────────────────────────────────────────┐
│                    NAVIGATEUR WEB (Frontend)                │
│                    http://localhost:5000                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ index.html (Interface Utilisateur)                   │   │
│  │  • 5 Sections (Accueil, Bacs, Détections, etc)     │   │
│  │  • Responsive design sans animations                │   │
│  │  • Badges de statut avec couleurs                   │   │
│  └──────────────────────────────────────────────────────┘   │
│              ↓                              ↓                 │
│  ┌──────────────────────┐      ┌────────────────────────┐   │
│  │ script.js (Logique)  │      │ style.css (Styles)     │   │
│  │ • fetch() API calls  │      │ • Responsive grid      │   │
│  │ • Polling 2/3/5 sec  │      │ • Badges colorés       │   │
│  │ • Logs horodatés     │      │ • Boutons désactivés   │   │
│  │ • Gestion événements │      └────────────────────────┘   │
│  └──────────────────────┘                                    │
│              │                                                 │
│              │ HTTP/JSON                                       │
│              ↓                                                 │
└─────────────────────────────────────────────────────────────┘
              │
              │ REST API Calls
              ↓
┌─────────────────────────────────────────────────────────────┐
│                   SERVEUR FLASK (Backend)                    │
│                    python app.py:5000                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Flask Application (app.py - 343 lignes)             │    │
│  └─────────────────────────────────────────────────────┘    │
│                         ↓                                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 9 Routes / APIs Implémentées                        │   │
│  │ ─────────────────────────────────────────────────── │   │
│  │ GET  /api/system/info         → psutil             │   │
│  │ GET  /api/gpu/info            → pynvml (GPU)       │   │
│  │ GET  /api/scripts/status      → psutil (procs)     │   │
│  │ GET  /api/scripts/run/<s>     → subprocess (new)   │   │
│  │ GET  /api/scripts/stop/<s>    → subprocess (kill)  │   │
│  │ GET  /api/processes           → psutil (list)      │   │
│  │ GET  /api/config/read         → File I/O           │   │
│  │ POST /api/config/save         → File I/O           │   │
│  │ GET  /api/camera/status       → Placeholder        │   │
│  │ GET  /api/arduino/status      → Placeholder        │   │
│  └──────────────────────────────────────────────────────┘   │
│                         │                                     │
└─────────────────────────┼─────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ↓               ↓               ↓
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │  psutil  │    │  pynvml  │    │subprocess│
    │ (System) │    │  (GPU)   │    │(Process) │
    │          │    │          │    │          │
    │CPU, RAM  │    │Temp, VM  │    │Run, Kill │
    │Disk, OsA │    │Util%, Mdl│    │Monitor   │
    └──────────┘    └──────────┘    └──────────┘
          │               │               │
          └───────────────┼───────────────┘
                          ↓
                  ┌─────────────────┐
                  │   SYSTÈME OS    │
                  │   (Windows 11)  │
                  │                 │
                  │ 12 cores @ 2904 │
                  │ 23.87 GB RAM    │
                  │ 1024 GB Disk    │
                  │ Uptime: 2h 45m  │
                  └─────────────────┘
```

---

## Timeline de Mise à Jour

```
┌────────────────────────────────────────────────────────┐
│ 📊 SYSTÈME INFO (5 sec)                                │
│ ┌──────────────────────────────────────────────────┐  │
│ │ CPU: 27.2% | RAM: 50.3% | Disk: 11.2%          │  │
│ └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
          ↓ every 5 seconds

┌────────────────────────────────────────────────────────┐
│ 🎮 GPU INFO (3 sec)                                    │
│ ┌──────────────────────────────────────────────────┐  │
│ │ Modèle: N/A | Temp: N/A | VRAM: N/A            │  │
│ └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
          ↓ every 3 seconds

┌────────────────────────────────────────────────────────┐
│ 🔧 SCRIPTS STATUS (2 sec) ← PLUS FRÉQUENT             │
│ ┌──────────────────────────────────────────────────┐  │
│ │ test_app.py: 🔴 Arrêté                          │  │
│ │ test_hardware.py: 🔴 Arrêté                     │  │
│ │ run_auto.sh: 🔴 Arrêté                          │  │
│ │ run_manual.sh: 🔴 Arrêté                        │  │
│ └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
          ↓ every 2 seconds
```

---

## État des Boutons (State Machine)

```
                 ┌─────────────┐
              ┌──│ UTILISATEUR  │──┐
              │  └─────────────┘  │
              │                   │
              ↓                   ↓
       ┌────────────┐      ┌────────────┐
       │ Clique sur │      │ Clique sur │
       │ "Lancer"   │      │ "Stop"     │
       │            │      │            │
       └────────────┘      └────────────┘
              │                   │
              ↓                   ↓
    ┌──────────────────┐  ┌──────────────────┐
    │ Vérifie si       │  │ Cherche le       │
    │ déjà en cours    │  │ processus        │
    │ (API status)     │  │ (API stop)       │
    └──────────────────┘  └──────────────────┘
              │                   │
              ↓                   ↓
    ┌──────────────────┐  ┌──────────────────┐
    │ Lance le script  │  │ Arrête gracieusement
    │ (subprocess)     │  │ (SIGTERM + timeout) │
    │ PID = NEW        │  │ PID = None           │
    └──────────────────┘  └──────────────────┘
              │                   │
              ↓                   ↓
    ┌──────────────────┐  ┌──────────────────┐
    │ Actualise statut │  │ Actualise statut │
    │ Badge 🟢 COURS   │  │ Badge 🔴 ARRÊTÉ  │
    │ Bouton Stop ON   │  │ Bouton Lancer ON │
    │ Bouton Lancer OFF│  │ Bouton Stop OFF  │
    └──────────────────┘  └──────────────────┘
              │                   │
              └───────┬───────────┘
                      │
                      ↓
            ┌──────────────────┐
            │ CONSOLE affiche  │
            │ [HH:MM:SS] [INFO]│
            │ logs horodatés   │
            └──────────────────┘
```

---

## Cycle de Vie d'une Requête

```
┌──────────────────────────────────────────────────────────┐
│ 1. UTILISATEUR CLIQUE "Lancer test_app.py"              │
└──────────────────────────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────────────┐
│ 2. FETCH API                                            │
│    fetch('/api/scripts/run/test_app.py')               │
└──────────────────────────────────────────────────────────┘
                         ↓
                    (HTTP POST)
                         ↓
┌──────────────────────────────────────────────────────────┐
│ 3. FLASK TRAITE LA REQUÊTE                              │
│    @app.route('/api/scripts/run/<script>')              │
│    - Vérifie si le script existe                        │
│    - Vérifie si pas déjà en cours                       │
│    - Lance subprocess.Popen([python, path])             │
│    - Retourne JSON success=true                         │
└──────────────────────────────────────────────────────────┘
                         ↓
                    (JSON Response)
                         ↓
┌──────────────────────────────────────────────────────────┐
│ 4. JAVASCRIPT TRAITE LA RÉPONSE                         │
│    - Affiche "[HH:MM:SS] [INFO] test_app lancé"         │
│    - Appelle updateScriptsStatus()                      │
└──────────────────────────────────────────────────────────┘
                         ↓
                    (next tick)
                         ↓
┌──────────────────────────────────────────────────────────┐
│ 5. ACTUALISE L'ÉTAT (polling 2 sec)                     │
│    fetch('/api/scripts/status')                         │
│    - Reçoit: {"test_app.py": {running: true, pid: 1234}}
│    - Met à jour le badge: "🟢 EN COURS (PID: 1234)"     │
│    - Désactive bouton "Lancer"                          │
│    - Active bouton "Stop"                               │
└──────────────────────────────────────────────────────────┘
                         ↓
                       ✅ SUCCÈS
```

---

## Carte des Responsabilités

| Composant | Responsabilité | Technologie |
|-----------|-----------------|------------|
| **Frontend (HTML)** | Affichage de l'UI | HTML5 + Semantic |
| **Styles (CSS)** | Présentation visuelle | CSS3 Responsive |
| **JS (Logic)** | Interactions + Polling | Vanilla JS / Fetch |
| **Flask (Backend)** | Logique métier + APIs | Python 3.12 |
| **psutil** | Infos système | Library |
| **pynvml** | Infos GPU | NVIDIA Driver |
| **subprocess** | Gestion processus | Python stdlib |
| **File I/O** | Lecture/Écriture config | Python stdlib |

---

## Points d'Intégration Futurs

```
┌────────────────────────────────────────────────┐
│ INTERFACE ADMIN v2.0 (ACTUELLEMENT)           │
├────────────────────────────────────────────────┤
│ ✅ Infos Système                              │
│ ✅ Gestion Scripts                            │
│ ✅ Configuration                              │
│ ⏳ Caméra (OpenCV) ← À intégrer               │
│ ⏳ Arduino (PySerial) ← À intégrer            │
│ ⏳ Capteurs (GPIO) ← À intégrer               │
│ ⏳ YOLO (Détections) ← À intégrer             │
│ ⏳ Base de Données ← À intégrer               │
└────────────────────────────────────────────────┘
           Voir INTEGRATION_GUIDE.md
```

---

## Sécurité & Déploiement

```
ACTUEL (Développement)
├── Pas d'authentification
├── HTTP non-chiffré
├── Debug Mode = ON
└── Accessible sur tout le réseau

RECOMMANDÉ (Production)
├── Ajouter Flask-Login
├── HTTPS avec certificats
├── Debug Mode = OFF
├── Firewall + authentification
└── WSGI server (Gunicorn, uWSGI)
```

---

## 📈 Performance

| Opération | Temps | Source |
|-----------|-------|--------|
| Récup sys info | ~100ms | psutil |
| Récup GPU info | ~50ms | pynvml |
| Récup scripts status | ~80ms | psutil |
| Affichage UI | <16ms | Navigateur |
| Lancer script | ~200ms | subprocess |
| Arrêter script | ~300ms | subprocess |

---

**Schéma créé:** 31/01/2026  
**Auteur:** Système Admin  
**Status:** ✅ Production Ready
