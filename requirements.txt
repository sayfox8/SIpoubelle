# Smart Bin SI - Dépendances Python
# YOLO = YOLOv5 via torch.hub (pas de package ultralytics)

# Série (Arduino)
pyserial>=3.5

# Données et images
numpy>=1.19.0
Pillow>=8.0.0
opencv-python>=4.5.0

# PyTorch (installé séparément sur Jetson via wheel)
# torch>=1.9.0
# torchvision>=0.10.0

# Optionnel
matplotlib>=3.3.0
pandas>=1.3.0
